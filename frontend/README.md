# Manager Frontend (Vite + React)

Quick start:

1. Open a terminal in this folder
2. npm install
3. npm run dev

The dev server will run on http://localhost:5173 by default and the backend is expected at http://localhost:8080/api
